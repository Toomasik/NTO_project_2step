/*
package ru.myitschool.work.data.repo

import ru.myitschool.work.data.repo.AuthRepository.codeCache
import ru.myitschool.work.data.source.NetworkDataSource

object MainRepository {
    private val BookingList: ArrayList<>? = null

    suspend fun getUserBooking() {return NetworkDataSource.getBooking().onSuccess { success ->
        if (success) {
            codeCache = text
        }
    }


}*/

/** там заглушка и по видимому она хочет id пользователя в бд*/